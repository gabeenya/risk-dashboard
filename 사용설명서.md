# 외식BG 리스크 관리 시스템 — 사용 설명서

> Risk Monitoring & Analytics Platform
> 이랜드 외식BG RO실 리스크 모니터링·분석 플랫폼

이 문서는 시스템을 처음 사용하는 분을 위한 안내서입니다. **가입 → 로그인 → 대시보드 조회 → 데이터 입력 → AI 분석 → 보고서 생성 → 관리자 기능** 순서로, 화면에 보이는 그대로 설명합니다.

---

## 0. 시작하기 전에 — 한눈에 보는 구조

화면은 크게 **왼쪽 사이드바**와 **본문**으로 나뉩니다.

- **왼쪽 사이드바**: 로고(클릭 시 대시보드로 이동) · `메뉴`(대시보드/데이터 입력/AI 분석/보고서 생성/사용 설명서/관리자) · `리스크 영역 현황`(영역별 누적 건수 바로가기) · 하단 내 계정·🔑 비밀번호·⎋ 로그아웃
- **상단 헤더**: 시스템 제목, 현재 페이지, 동기화 상태, 새로고침(↻)

### 권한(역할) 구분

| 역할 | 표시 | 할 수 있는 일 |
|---|---|---|
| 브랜드장 | 조회 전용 | 본인에게 허용된 **브랜드·영역**만 대시보드/AI에서 조회 |
| 관리자 | 관리자 | 전체 브랜드 조회 + **데이터 입력** + **보고서 생성** |
| 최상위 관리자 | OWNER | 위 모든 권한 + **관리자 페이지**(사용자 승인·계정 관리·알림 임계값) |

> - **데이터 입력**은 관리자 이상만 사용할 수 있습니다.
> - **관리자 페이지**는 지정된 최상위 관리자 계정에게만 사이드바에 노출됩니다.

### 11개 리스크 영역 · 3개 분류

| 분류 | 소속 영역 |
|---|---|
| 컴플라이언스 | 불법파견 · 표시광고 · 가맹 · IP · 노무 · 영업비밀 |
| 부정/부실 제거 | 부실채권 · 감사 |
| 매장 운영 관리 | 위생 · 안전 · 클레임 |

### 처리 상태

| 영역 | 상태 라벨(3단계) |
|---|---|
| 일반 영역(불법파견·표시광고·가맹·IP·노무·영업비밀·위생) | 모니터링 → 위반(처리중) → 완료 |
| 클레임 | 접수 → 처리중 → 처리완료 |
| 안전 | 발생 → 발생(처리중) → 조치완료 |
| 감사 | 적발 → 적발(처리중) → 조치완료 |
| 부실채권 | 발생 → 발생(처리중) → 해결 |

---

## 1. 가입 신청

1. 로그인 화면 하단 **"계정이 없으신가요? 가입 신청"** 클릭
2. **이름 / 아이디(영문·숫자, `. - _` 가능) / 비밀번호(4자 이상) / 희망 접근 브랜드**(1개 이상, `모두 선택`/`모두 해제` 제공) 입력
3. **가입 신청** 클릭 → 접수 완료 안내 확인

> ⚠️ 신청만으로는 로그인되지 않습니다. **관리자 승인** 후 로그인 가능하며, 승인 단계에서 접근 브랜드·영역이 최종 확정됩니다.

---

## 2. 로그인 · 세션 · 비밀번호

| 항목 | 내용 |
|---|---|
| 로그인 | 아이디·비밀번호 입력 → 로그인(또는 Enter) → 대시보드로 자동 이동 |
| 세션 유지 | 새로고침해도 로그인 유지 |
| 자동 로그아웃 | 로그인 후 **3시간** 경과 시 자동 로그아웃(만료 5분 전 경고 팝업 — 세션 연장/즉시 로그아웃 선택) |
| 비밀번호 변경 | 사이드바 하단 **🔑 비밀번호** → 현재 비밀번호 확인 후 변경(4자 이상) |
| 로그아웃 | 사이드바 하단 **⎋ 로그아웃** |

---

## 3. 대시보드 — 현황 한눈에 보기

로그인 후 가장 먼저 보이는 화면입니다.

### 3-1. 분류 · 기준 월 · 필터

| 필터 | 옵션 |
|---|---|
| 분류 | 전체 분류 / 컴플라이언스 / 부정·부실 제거 / 매장 운영 관리 (선택 시 영역 필터가 좁혀짐) |
| 기준 월 | 조회할 월 선택 |
| 영역 | 11개 영역 중 하나 또는 전체 영역 |
| 브랜드 | 본인 권한 내 브랜드 또는 전체 브랜드 |

사이드바 `리스크 영역 현황`의 영역 이름을 눌러도 같은 필터가 걸립니다.

### 3-2. 상단 알림 바

당월 위반이 전월 대비 임계값(7-4장) 이상 급증한 영역이 있으면 화면 상단에 **빨간 알림 바**가 표시됩니다. 알림 칩을 클릭하면 해당 영역으로 바로 필터가 이동합니다.

### 3-3. 리스크 노출/측정판

브랜드별 리스크 등급을 **A~F**로 산정해 순위로 보여주는 표입니다.

| 항목 | 내용 |
|---|---|
| 평가 대상 | 불법파견·표시광고·가맹·IP·노무·영업비밀·부실채권·안전 8개 영역(위생·클레임·감사는 별도 관리되어 제외) |
| 표시 내용 | 영역별 등급+건수, 8개 영역 평균 점수로 산정한 종합등급·순위(1~3위 강조) |
| 등급 기준 확인 | 표 우측 상단 `?` 아이콘 클릭 |
| 기간 설정 | 누적/당월 토글 + 기간(from~to) 직접 설정 |
| 브랜드 단독 선택 시 | 순위 없이 해당 브랜드만의 측정판으로 전환 |

**등급 산정 기준** (선택한 기간의 위반율 = 위반+완료 건수 ÷ 전체(모니터링+위반+완료) 건수 기준)

| 영역 | A | B | C | D | F(최우선 적용) |
|---|---|---|---|---|---|
| 컴플라이언스 6개 영역(불법파견·표시광고·가맹·IP·노무·영업비밀) | 위반율 10% 이하 | 10% 초과 20% 이하 | 20% 초과 30% 이하 | 30% 초과 50% 이하 | 위반율 50% 초과 또는 외부노출 1건 이상이면 F |
| 안전 | 위반율 10% 이하 | 10% 초과 20% 이하 | 20% 초과 30% 이하 | 30% 초과 50% 이하 | 위반율 50% 초과 또는 중대재해 발생 1건 이상이면 F |
| 부실채권 | 위반율 10% 이하 | 10% 초과 20% 이하 | 20% 초과 30% 이하 | 30% 초과 50% 이하 | 위반율 50% 초과 또는 2개월 초과 미입금 건이 있으면 D(금액 1억 이하)·F(금액 1억 초과) |

**점수 산정 방법**

1. 등급별 점수: `A=10점 · B=8점 · C=5점 · D=3점 · F=0점`
2. 브랜드별 **종합점수** = 평가 대상 8개 영역 등급 점수의 평균
3. **종합등급**: 평균 9~10점→A, 7~8점→B, 4~6점→C, 1~3점→D, 0점→F
4. **100점 환산 점수** = 평균점수 × 10 (표에 표시되는 값)

### 3-4. 조치사항

| 구성 | 내용 |
|---|---|
| 징계 현황 | 감사 영역 징계 기록(날짜·브랜드·상세유형·징계유형·성명·양형·비고·상태) 표. 완전 전체 뷰·부정/부실 제거 분류뷰·감사 영역뷰에서만 노출 |
| 영역별 특이사항 | 데이터 입력 탭에서 등록한 주요이슈·이슈상세·조치완료 사항을 최신순으로 표시 |
| 감사 KPI 카드 4개 | 조치완료/적발 · 경징계 건수 · 중징계 건수 · 형사고발 건수 — **분류·영역 모두 `전체`일 때만** 노출, 감사 데이터 연 누적 기준(브랜드 필터 반영) |

두 표 모두 누적/당월 토글과 기간 설정을 지원합니다.

> 📌 분류나 영역을 좁히면 **측정판과 조치사항이 하나의 카드로 합쳐져** 나란히 표시됩니다. 분류·영역을 모두 `전체`로 둔 **완전 전체 화면**에서만 두 개의 독립 카드로 분리됩니다.

### 3-5. KPI 카드 4개

완전 전체 뷰(분류·영역 모두 `전체`)에서는 측정판 **위쪽**, 그 외 필터 뷰에서는 측정판 **아래쪽**에 표시됩니다. 선택한 뷰에 따라 4개 카드의 라벨과 값 형식이 달라집니다.

| 선택한 뷰 | KPI1(연 누적) | KPI2(당월) | KPI3 | KPI4 |
|---|---|---|---|---|
| 완전 전체 · 컴플라이언스 분류뷰 · 개별 영역(불법파견·표시광고·가맹·IP·노무·영업비밀·위생) | 위반/모니터링(위반율 %) | 위반/모니터링(위반율 %) | 완료율 | 조치중 건수 |
| 클레임 | 처리완료/접수·처리중(처리율 %) | 동일(당월 기준) | *(숨김 — 카드 3개만 표시)* | 처리중 건수 |
| 안전 | 조치완료/발생(조치율 %) | 동일(당월 기준) | *(숨김 — 카드 3개만 표시)* | 발생 건수 |
| 감사 | 연 누적 징계 건수(조치완료 O건) | 당월 조치완료 건수(적발 O건) | 조치완료율 | 현재 적발 현황 |
| 부실채권 | 연 누적 발생 건수(해결 O건) | 당월 발생 건수(발생 O건) | 누적 회수금액 | 현재 발생 현황 |
| 매장 운영 관리 분류뷰(전체 영역, 안전+클레임 합산·위생 제외) | 해결/발생(해결율 %) | 동일(당월 기준) | 해결율 | 조치중 건수 |
| 부정/부실 제거 분류뷰(전체 영역, 감사+부실채권 합산) | 연 누적 건수(완료 O건) | 당월 발생 건수(조치중 O건) | 누적 회수 금액 | 징계 조치완료율 |

### 3-6. 차트

| 차트 | 내용 |
|---|---|
| 월별 모니터링/위반 건수 추이(라인) | 월별 흐름 |
| 위반 유형별 분포(도넛) | 영역·상세유형 비중 |
| 브랜드별 현황(바) | 브랜드별 모니터링·위반 비교 |

> 📌 **영업비밀 환산 안내**: 영업비밀의 *모니터링 건수*는 그래프에서 **10:1로 환산**되어 표시됩니다(위반 건수는 1:1 그대로). 예) 실제 모니터링 1,540건·위반 3건 → 그래프에는 154건·3건.

### 3-7. 최근 모니터링 현황 (표)

날짜·유형·상세·브랜드·상태·비고를 최신순으로 표시. 상태별 필터(**전체/모니터링/위반(처리중)/완료**) + 페이지 번호.

### 3-8. 브랜드/영역별 위험도 히트맵

위반이 많은 브랜드·상위 영역을 색 농도로 표시. **카드/칩 클릭** → 오른쪽 드릴다운 패널에 상세 기록(날짜·상세유형·브랜드·건수·상태·작성자·비고) 표시, `✕` 로 닫기.

---

## 4. 데이터 입력 *(관리자 이상)*

사이드바 **데이터 입력** 탭. 맨 위 **분류·리스크 영역**을 먼저 선택하면 이후 입력이 그 영역 기준으로 동작합니다.

### 4-1. 엑셀 일괄 업로드

1. **📥 양식 다운로드**(현재 선택된 영역 전용 양식)
2. 양식의 **`데이터` 시트**만 채움(첫 행은 헤더, 둘째 행부터 입력) — 같은 양식으로 **여러 영역 동시 업로드 가능**
3. **📂 엑셀 파일 선택(.xlsx)**
4. **미리보기**에서 `✓ 유효` / `✕ 오류` / `전체 행` 수 확인
5. **✓ 유효 건만 업로드** (오류 행은 사유 표시, `취소` 가능)

### 4-2. 신규 입력 (한 건씩)

**날짜 / 위반 유형(선택 영역 자동) / 상세 위반 유형 / 브랜드 / 상태 / 건수** 를 채우고 **입력하기**(`초기화` 로 비움).

| 추가 입력 항목 | 표시 조건 |
|---|---|
| 외부노출 여부(체크박스) | 모든 영역에서 표시 |
| 징계 대상자 성명 · 양형 · 징계유형(금전회수/경징계/중징계/형사고발) | **감사** 영역일 때만 |
| 금액 | **부실채권**이고 상세유형이 `미입금` 또는 `2개월 초과 미입금`일 때만 |
| 비고(선택) | 모든 영역 |

### 4-3. 영역별 특이사항 입력

**날짜·브랜드(선택)·주요이슈·이슈상세·조치완료 사항**을 입력해 등록. 목록에서 바로 **수정·저장·삭제** 가능하며, 대시보드 조치사항판의 "영역별 특이사항"에도 함께 반영됩니다.

### 4-4. 데이터 목록 (조회·수정·삭제)

- **상세유형·브랜드·건수·상태·비고**는 목록에서 **바로 클릭해 수정**.
- 상단 필터: **상세유형 / 브랜드 / 상태**(`필터 초기화` 제공), 현재 건수 표시.
- **여러 건 선택**(체크박스): **선택 상태 변경** / **선택 삭제**.

---

## 5. AI 리스크 분석

사이드바 **AI 분석** 탭에서, 필요한 부분만 골라 분석 리포트를 받습니다.

| 단계 | 내용 |
|---|---|
| 1. 분석 대상 브랜드 선택 | 미선택 시 전체 브랜드 기준 |
| 2. 분석 기간 설정 | 시작일~종료일 직접 지정(비워두면 전체 기간, 좁힐수록 빠름, `초기화` 제공) |
| 3. 분석 항목 선택(복수 가능) | 위험도 식별 / 트렌드 분석 / 완료율 / 권고사항 / 액션 플랜 / 브랜드별 비교 (위험도·트렌드 기본 선택) |
| 추가 컨텍스트(선택, 화면 상단) | 분석 관점을 한 줄로. 예) *"브랜드장 입장에서 우리 브랜드 리스크 관리 수준 분석"* |
| 출력 스타일(화면 상단) | 📝 노션 / 📰 신문 / 💻 터미널 |
| 실행 | **✦ AI 분석 시작** — 약 5~30초 소요, 결과는 하단에 표시 |

> 💡 한 번에 너무 많은 항목보다 **궁금한 항목만 골라** 짧게 돌리면 더 빠르고 또렷한 결과를 얻습니다.

---

## 6. 보고서 생성 (PPT) *(관리자 이상)*

사이드바 **보고서 생성** → 모달에서 **기준 월**(비워두면 지난 달) 선택 → **보고서 생성** → PPTX 자동 다운로드.

| 순서 | 슬라이드 | 내용 |
|---|---|---|
| 1 | 표지 | — |
| 2 | 고정 슬라이드 | 정해진 서식의 컴플라이언스 핵심 지표·주차별 실적표 |
| 3 | 리스크 노출/측정판(연 누적) | 대시보드 측정판과 동일한 브랜드별 등급 순위표 |
| 4 | 리스크 노출/측정판(당월) | 위와 동일, 당월 데이터 |
| 5 | 조치사항(연 누적) | 징계 현황 + 영역별 특이사항 |
| 6 | 조치사항(당월) | 위와 동일, 당월 데이터 |
| 7 | 법인 전체 개요 | KPI 4개 + 월별 추이 |
| 8~9 | 브랜드별 종합(연누적/전월) | 영역별 전체/위반 + 합계 |
| 10~ | 영역별 상세 | 11개 영역마다 표 + 도넛 + 핵심 지표 |
| 이후 | 영역별 요약 카드 / 브랜드별 상세 | — |

---

## 7. 관리자 페이지 *(최상위 관리자 전용)*

### 7-1. 가입 신청 대기 (승인/거절)
- 신청이 있으면 대기 목록 표시(없으면 숨김).
- **승인** 클릭 → 팝업에서 **접근 브랜드·확인 가능 영역**을 선택(신청 시 고른 브랜드 미리 체크)해 확정 → 즉시 로그인 가능.
- **거절** → 신청 삭제.

### 7-2. 새 계정 직접 추가
1. 이름 · 아이디 · 임시 비밀번호(4자 이상) 입력
2. **권한** 선택 — `브랜드장(조회 전용)`(접근 브랜드·영역 지정) 또는 `관리자(입력/보고서 포함, 전체 브랜드)`
3. **계정 추가** → 생성 정보를 **복사**해 전달

### 7-3. 가입된 사용자 관리

| 항목 | 내용 |
|---|---|
| 이름 | ✎ 클릭해 수정 |
| 권한 | 드롭다운으로 브랜드장/관리자 전환 |
| 접근 브랜드 · 접근 영역 | ✎ 클릭해 팝업에서 수정(관리자 권한은 자동으로 "전체") |
| 삭제 | 관리자(admin) 계정을 제외한 사용자를 목록에서 바로 삭제(✕) 가능 |

### 7-4. 영역별 알림 임계값
- 영역마다 **당월 위반 +건수** 또는 **+%** 임계값 설정.
- 당월 위반이 임계값을 초과하면 대시보드 상단에 알림 바 표시(전월 대비 증가분 기준).
- **임계값 저장** / **기본값 복원** 제공.

---

## 8. 자주 묻는 질문 (FAQ)

**Q. 가입했는데 로그인이 안 됩니다.**
→ 관리자 승인 전입니다. 승인되면 바로 로그인됩니다.

**Q. 데이터 입력 / 보고서 생성 탭이 동작하지 않아요.**
→ 해당 기능은 **관리자 이상** 권한이 필요합니다. 권한 변경은 관리자에게 요청하세요.

**Q. 일부 브랜드/영역이 화면에 안 보여요.**
→ 브랜드장은 **승인된 브랜드·영역만** 보입니다. 추가 권한은 관리자에게 요청하세요.

**Q. 갑자기 로그아웃되었습니다.**
→ 보안상 로그인 후 3시간이 지나면 자동 로그아웃됩니다. 다시 로그인하세요.

**Q. 영업비밀 모니터링 숫자가 실제보다 적게 보입니다.**
→ 그래프에서 **모니터링 건수만 10:1로 환산**해 표시한 것입니다. 위반 건수는 실제 값입니다.

**Q. 리스크 노출/측정판과 조치사항이 왜 어떤 화면에서는 나란히, 어떤 화면에서는 따로 보이나요?**
→ 분류·영역을 `전체`로 둔 **완전 전체 화면**에서만 두 개의 독립 카드로 분리됩니다. 분류나 영역을 좁히면 하나의 카드로 합쳐져 나란히 표시됩니다.

**Q. 최신 데이터가 안 보여요.**
→ 상단 헤더의 **↻ 새로고침** 을 눌러 다시 불러오세요.

---

*문의나 권한 요청은 시스템 관리자에게 연락해 주세요.*
