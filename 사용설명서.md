# 외식BG 리스크 관리 시스템 — 사용 설명서

> Risk Monitoring & Analytics Platform
> 이랜드 외식BG RO실 리스크 모니터링·분석 플랫폼

이 문서는 시스템을 처음 사용하는 분을 위한 안내서입니다. **가입 → 로그인 → 대시보드 조회 → 데이터 입력 → AI 분석 → 보고서 생성 → 관리자 기능** 순서로, 화면에 보이는 그대로 설명합니다.

---

## 0. 시작하기 전에 — 한눈에 보는 구조

화면은 크게 **왼쪽 사이드바**와 **본문**으로 나뉩니다.

- **왼쪽 사이드바**
  - 상단: 로고(클릭 시 대시보드로 이동)
  - `메뉴`: 대시보드 · 데이터 입력 · AI 분석 · 보고서 생성 · 관리자
  - `리스크 영역 현황`: 11개 영역별 현재 건수 바로가기
  - 하단: 내 계정 정보 · 🔑 비밀번호 · ⎋ 로그아웃
- **상단 헤더**: 시스템 제목, 현재 페이지 표시, 동기화 상태, 새로고침(↻) 버튼

### 권한(역할) 구분

| 역할 | 표시 | 할 수 있는 일 |
|---|---|---|
| **브랜드장** | `조회 전용` | 본인에게 허용된 **브랜드·영역**만 대시보드/AI에서 조회 |
| **관리자** | `관리자` | 전체 브랜드 조회 + **데이터 입력** + **보고서 생성** |
| **최상위 관리자** | — | 위 모든 권한 + **관리자 페이지**(사용자 승인·계정 관리·알림 임계치) |

> - **데이터 입력** 탭은 관리자 이상만 사용할 수 있습니다(브랜드장이 눌러도 권한 안내가 뜹니다).
> - **관리자 페이지**는 최상위 관리자에게만 사이드바에 노출됩니다.

### 11개 리스크 영역

불법파견 · 표시광고 · 가맹 · IP · 노무 · 영업비밀 · 부실채권 · 위생 · 안전 · 클레임 · 징계

### 처리 상태(3단계)

`모니터링` → `위반(처리중)` → `완료`
(클레임 영역은 별도로 `접수` · `처리중` · `처리완료` 상태를 사용합니다.)

---

## 1. 가입 신청

1. 로그인 화면 하단의 **"계정이 없으신가요? 가입 신청"** 을 클릭합니다.
2. 아래 항목을 입력합니다.
   - **이름**: 실제 이름
   - **아이디**: 영문/숫자 조합 (`. - _` 사용 가능)
   - **비밀번호**: 4자 이상
   - **희망 접근 브랜드**: 본인이 조회하려는 브랜드를 1개 이상 선택 (`모두 선택`/`모두 해제` 버튼 제공)
3. **가입 신청** 버튼을 누릅니다.
4. *"가입 신청 완료 — 관리자 승인 후 로그인 가능합니다."* 안내가 뜨면 정상 접수된 것입니다.

> ⚠️ 신청 직후에는 로그인이 되지 않습니다. **관리자가 승인**해야 로그인할 수 있고, 승인 단계에서 접근 브랜드·영역 권한이 최종 확정됩니다. 승인 전 로그인 시 *"관리자 승인 대기 중인 계정입니다."* 가 표시됩니다.

---

## 2. 로그인 · 세션 · 비밀번호

### 로그인
- 아이디와 비밀번호를 입력하고 **로그인** 버튼(또는 비밀번호 칸에서 Enter)을 누릅니다.
- 로그인하면 자동으로 **대시보드**로 이동합니다.

### 세션 유지 / 자동 로그아웃
- 브라우저를 새로고침해도 로그인이 유지됩니다.
- **보안을 위해 로그인 후 3시간이 지나면 자동 로그아웃**됩니다.
- 만료 **5분 전 경고 팝업**이 뜹니다.
  - **세션 연장 (3시간)**: 3시간 다시 연장
  - **지금 로그아웃**: 즉시 종료

### 비밀번호 변경
- 사이드바 하단 **🔑 비밀번호** → 현재 비밀번호 확인 후 새 비밀번호(4자 이상)로 변경합니다.

### 로그아웃
- 사이드바 하단 **⎋ 로그아웃** 을 누르면 로그인 화면으로 돌아갑니다.

---

## 3. 대시보드 — 현황 한눈에 보기

로그인 후 가장 먼저 보이는 화면입니다. 상단부터 차례로 설명합니다.

### 3-1. 기준 월 · 필터
- **기준 월**: 조회할 월을 선택합니다. **당월** 버튼으로 이번 달로 되돌립니다.
- **영역** 필터: 11개 영역 중 하나 또는 `전체 영역`
- **브랜드** 필터: 본인 권한 내 브랜드 또는 `전체 브랜드`
- (사이드바 `리스크 영역 현황`의 영역 이름을 눌러도 필터가 걸립니다.)

### 3-2. 상단 알림 바
당월 위반이 전월 대비 임계치 이상 급증한 영역이 있으면 화면 상단에 **빨간 알림 바**가 표시됩니다. (임계치는 관리자가 설정 — 7장 참고)

### 3-3. KPI 카드 4개
- **누적 위반/모니터링**: 연 누적 집계
- **당월 위반/모니터링**: 선택한 기준 월 집계
- **처리 완료율**: 전체 대비 완료 비율
- **현재 조치중 건수**: `위반(처리중)` 상태 건수

### 3-4. 차트
- **월별 모니터링/위반 건수 추이** (라인): 월별 흐름
- **위반 유형별 분포** (도넛): 영역 또는 상세유형 비중
- **브랜드별 현황** (바): 브랜드별 모니터링·위반 비교

> 📌 **영업비밀 환산 안내**: 영업비밀의 *모니터링 건수*는 그래프에서 **10:1로 환산**되어 표시됩니다(위반 건수는 1:1 그대로). 예) 실제 모니터링 1,540건·위반 3건 → 그래프에는 154건·3건으로 반영. 차트 하단 안내 문구로도 표시됩니다.

### 3-5. 최근 모니터링 현황 (표)
- 날짜·유형·상세·브랜드·상태·비고를 최신순으로 보여줍니다.
- 상단 버튼으로 상태별 필터: **전체 / 모니터링 / 위반(처리중) / 완료**
- 표 아래 **페이지 번호**로 넘겨봅니다.

### 3-6. 브랜드/영역별 위험도 히트맵
- 위반이 많은 브랜드와 상위 영역을 색 농도로 표시합니다.
- **카드나 칩을 클릭**하면 오른쪽에 **드릴다운 패널**이 열려 해당 조합의 상세 기록(날짜·상세유형·브랜드·건수·상태·작성자·비고)을 볼 수 있습니다. `✕` 로 닫습니다.

---

## 4. 데이터 입력 *(관리자 이상)*

사이드바 **데이터 입력** 탭에서 진행합니다. 맨 위 **리스크 영역**을 먼저 선택하면, 이후 모든 입력이 그 영역 기준으로 동작합니다.

### 4-1. 엑셀 일괄 업로드 (여러 건 한 번에)
1. **📥 양식 다운로드** 로 표준 엑셀 양식을 받습니다.
2. 양식의 **`데이터` 시트**만 채웁니다. **첫 행(헤더)은 그대로 두고 둘째 행부터** 입력합니다.
   - 같은 양식으로 가맹·불법파견 등 **여러 영역을 한 번에** 올릴 수 있습니다.
3. **📂 엑셀 파일 선택 (.xlsx)** 으로 파일을 고릅니다.
4. **미리보기**가 뜹니다 — `✓ 유효`, `✕ 오류`, `전체 행` 수를 확인합니다.
5. **✓ 유효 건 업로드** 를 누르면 유효한 행만 등록됩니다. (오류 행은 검증 사유 표시 / `취소` 가능)

### 4-2. 안전 점검표 업로드 *(영역이 '안전'일 때만 표시)*
- 안전 점검표 파일의 **첫 번째 시트**만 읽습니다.
- 매장(행)별 점검 항목을 자동 전개하여 등록합니다.
  - 항목 값이 **0이 아니면 → 위반(처리중)**, **0이면 → 모니터링**, **빈 칸은 제외**
  - 영역은 **안전** 고정, 매장명은 비고로 저장됩니다.
- ⚠️ **문서보안(DRM)이 걸린 파일은 읽을 수 없습니다** — 보안 해제(반출)된 사본으로 업로드하세요.

### 4-3. 신규 입력 (한 건씩)
다음 항목을 채우고 **입력하기** 를 누릅니다. (`초기화` 로 비움)
- **날짜 / 위반 유형(선택한 영역 자동) / 상세 위반 유형 / 브랜드 / 건수 / 상태 / 비고(선택)**

### 4-4. 데이터 목록 (조회·수정·삭제)
- **상세유형·브랜드·건수·상태·비고**는 목록에서 **바로 클릭해 수정**할 수 있습니다.
- 상단 필터: **상세유형 / 브랜드 / 상태** (`필터 초기화` 제공), 현재 건수 표시
- **여러 건 선택**(맨 앞 체크박스):
  - **선택 상태 변경**: 고른 건들의 상태를 한 번에 변경
  - **선택 삭제**: 고른 건들을 한 번에 삭제

---

## 5. AI 리스크 분석

사이드바 **AI 분석** 탭에서, 필요한 부분만 골라 분석 리포트를 받습니다.

1. **1. 분석 대상 브랜드 선택**
   - 분석할 브랜드를 고릅니다. **선택하지 않으면 전체 브랜드**가 대상입니다.
2. **2. 분석 항목 선택** (복수 선택 가능)
   - **위험도 식별 / 트렌드 분석 / 완료율 / 권고사항 / 액션 플랜 / 브랜드별 비교**
   - (`위험도 식별`·`트렌드 분석`은 기본 선택)
3. **추가 컨텍스트**(선택): 분석 관점을 한 줄로 적습니다.
   예) *"브랜드장 입장에서 우리 브랜드 리스크 관리 수준 분석"*
4. **출력 스타일** 선택: 📝 노션 / 📰 신문 / 💻 터미널
5. **✦ AI 분석 시작** 클릭.
   - 선택 항목 수에 따라 **약 5~30초** 소요됩니다. (항목이 적을수록 빠릅니다)
   - 결과는 화면 하단에 리포트로 표시됩니다.

> 💡 한 번에 너무 많은 항목을 고르기보다, **궁금한 항목만 골라** 짧게 돌리면 더 빠르고 또렷한 결과를 얻습니다.

---

## 6. 보고서 생성 (PPT) *(관리자 이상)*

사이드바 **보고서 생성** 을 누르면 모달이 열립니다.

1. **기준 월** 선택 (비워두면 **지난 달** 데이터로 생성)
2. **보고서 생성** 클릭 → PPTX 파일이 자동으로 만들어져 다운로드됩니다.

생성되는 슬라이드 구성:
1. **표지**
2. **법인 전체 개요** — KPI 4개 + 월별 추이
3. **브랜드별 종합** — 연누적·당월 × 영역별 전체/위반 + 합계
4. **영역별 상세** — 영역마다 표 + 도넛 + 핵심 지표

---

## 7. 관리자 페이지 *(최상위 관리자 전용)*

사이드바 **관리자** 탭에서 사용자와 알림을 관리합니다.

### 7-1. 가입 신청 대기 (승인/거절)
- 신청이 있으면 대기 목록이 표시됩니다(없으면 숨김).
- **승인**: 신청자에게 부여할 **접근 브랜드·확인 가능 영역**을 선택해 확정 → 즉시 로그인 가능
  - 신청 시 고른 브랜드가 미리 체크되어 있습니다.
- **거절**: 신청이 삭제됩니다.

### 7-2. 새 계정 직접 추가
1. 이름 · 아이디 · 임시 비밀번호(4자 이상) 입력
2. **권한** 선택
   - `브랜드장 (조회 전용)` → 아래에서 **접근 브랜드·확인 가능 영역** 지정
   - `관리자 (입력/보고서 포함, 전체 브랜드)`
3. **계정 추가** → 생성된 계정 정보를 **복사**해 팀원에게 전달합니다.

### 7-3. 가입된 사용자 관리
- 사용자 목록에서 **권한·접근 브랜드·접근 영역·이름**을 행에서 직접 수정합니다.

### 7-4. 영역별 알림 임계치
- 영역마다 **전월 대비 +건수** 또는 **+%** 임계치를 설정합니다.
- 당월 위반이 임계치 이상 증가하면 대시보드 상단에 **알림 바**가 뜹니다.
- **임계치 저장** / **기본값 복원** 버튼 제공.

---

## 8. 자주 묻는 질문 (FAQ)

**Q. 가입했는데 로그인이 안 됩니다.**
→ 관리자 승인 전입니다. 승인되면 바로 로그인됩니다.

**Q. 데이터 입력 / 보고서 생성 탭이 동작하지 않아요.**
→ 해당 기능은 **관리자 이상** 권한이 필요합니다. 권한 변경은 관리자에게 요청하세요.

**Q. 일부 브랜드/영역이 화면에 안 보여요.**
→ 브랜드장은 **승인된 브랜드·영역만** 보입니다. 추가 권한은 관리자에게 요청하세요.

**Q. 갑자기 로그아웃되었습니다.**
→ 보안상 로그인 후 3시간이 지나면 자동 로그아웃됩니다. 다시 로그인하세요.

**Q. 영업비밀 모니터링 숫자가 실제보다 적게 보입니다.**
→ 그래프에서 **모니터링 건수만 10:1로 환산**해 표시한 것입니다. 위반 건수는 실제 값입니다.

**Q. 안전 점검표 엑셀이 안 올라가요.**
→ 문서보안(DRM)이 걸린 파일은 읽지 못합니다. **보안 해제된 사본**으로 올려주세요.

**Q. 최신 데이터가 안 보여요.**
→ 상단 헤더의 **↻ 새로고침** 을 눌러 다시 불러오세요.

---

*문의나 권한 요청은 시스템 관리자에게 연락해 주세요.*
